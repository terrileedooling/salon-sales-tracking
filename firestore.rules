rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow ALL access for development/emulator
    match /{document=**} {
      allow read, write: if true;
    }
    
  }
}
